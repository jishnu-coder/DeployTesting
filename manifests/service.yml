apiVersion: v1
kind: Service
metadata:
    name: "weatherapi-3e54"
    labels:
        app: "weatherapi-3e54"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "weatherapi-3e54"