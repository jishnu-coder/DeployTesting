apiVersion: v1
kind: Service
metadata:
    name: "weatherapi-d1eb"
    labels:
        app: "weatherapi-d1eb"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "weatherapi-d1eb"